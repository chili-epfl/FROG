machine:
  node:
    version: 7.2.0

dependencies:
  override:
    - npm -g install npm@latest
    - curl https://install.meteor.com | /bin/sh
    - ./initial_setup.sh

test: 
  post:
    - npm test
    - cd frog && npm test

checkout:
  post:
    - git submodule update --init
