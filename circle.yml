machine:
  node:
    version: 0.10.43

dependencies:
  override:
    - npm -g install npm@latest
    - curl https://install.meteor.com | /bin/sh
    - ./initial_setup.sh

test: 
  post:
    - cd frog && npm test

checkout:
  post:
    - git submodule update --init
