machine:
  node:
    version: 0.10.43

dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - ./initial_setup.sh
    - cd frog && npm install

test: 
  post:
    - cd frog && npm test

checkout:
  post:
    - git submodule update --init
