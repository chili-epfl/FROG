machine:
  node:
    version: 0.10.43

dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - cd frog && npm install

test: 
  post: 
    - pwd

checkout:
  post:
    - git submodule update --init
