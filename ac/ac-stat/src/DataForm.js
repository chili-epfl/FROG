// @flow

import * as React from 'react';

export default () => (
  <div style={{ width: '25%' }}>
    <h3>Data</h3>
    Select a zone to zoom on it
    <br />
    Double click on the graph to zoom out
  </div>
);
