// @flow

import React from 'react';

const ActivityRunner = ({activityData, data, dataFn, userInfo}) => {
  console.log(dataFn);
  console.log(dataFn.importFile);
  return (
    <div>
    </div>
  );
};

export default ActivityRunner;
